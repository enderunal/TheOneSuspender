{
    "name": "una-suspender-tests",
    "version": "1.0.0",
    "description": "Tests for UnaSuspender Chrome Extension",
    "scripts": {
        "test": "playwright test",
        "test:headed": "playwright test --headed",
        "test:debug": "playwright test --debug",
        "test:ui": "playwright test --ui",
        "screenshots": "playwright test tests/auto-screenshots.test.js --headless"
    },
    "devDependencies": {
        "@playwright/test": "^1.40.0"
    }
}